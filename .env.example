# Copy this file to .env on the server (it is git-ignored).
# Values are fetched from AWS Secrets Manager by the deploy script.

VITE_ENVIRONMENT=development
VITE_API_BASE_URL=https://api-dev.zmhadvisors.com
VITE_NOTES_ENCRYPTION_KEY=replace-with-strong-fixed-key
